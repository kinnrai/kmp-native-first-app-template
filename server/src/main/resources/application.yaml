ktor:
  deployment:
    host: "$KTOR_HOST:0.0.0.0"
    port: "$PORT:8080"
  application:
    modules:
      - com.example.kmpnativefirst.ApplicationKt.module
storage:
  jdbcUrl: "$TASK_DATABASE_URL:jdbc:sqlite:data/tasks.db"
