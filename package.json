{
  "name": "kmp-native-first-app-template",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app/webApp",
    "app/sharedLogic/build/dist/js/productionLibrary"
  ],
  "overrides": {
    "ws": "8.21.1"
  },
  "scripts": {
    "build:shared": "./gradlew :app:sharedLogic:jsBrowserProductionLibraryDistribution",
    "dev": "npm run build:shared && npm run dev -w web-app",
    "build": "npm run build:shared && npm run build -w web-app",
    "preview": "npm run preview -w web-app"
  }
}
